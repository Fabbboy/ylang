pub mod ast_lower;
/*
use either::Either;
use smallvec::SmallVec;

#[derive(Debug)]
pub enum AstLoweringError {}

pub const MAX_INLINE_ERRORS: usize = 4;
pub struct AstLoweringErrorMMO(
  pub Either<AstLoweringError, SmallVec<[AstLoweringError; MAX_INLINE_ERRORS]>>,
);

*/
