func main(argc: i32, argv: i8**): i32 {
  var lol = 123 + 12;
  lol; // if semicolon is missing here error messages get misleading
} // we get a "expected Brace(false) but got eof" although we clearly have a RBrace here lol

// here better example 
func test() i8 {
  noissuehere;
  ohshitforgotsemicolon
  allgood; // now the error message is good because in this example we get the // "expected Semicolon but got Identifier" which is correct
}